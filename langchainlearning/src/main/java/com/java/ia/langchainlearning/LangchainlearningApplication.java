package com.java.ia.langchainlearning;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class LangchainlearningApplication {

    public static void main(String[] args) {
        SpringApplication.run(LangchainlearningApplication.class, args);

//        Map<String, String> myMap =  System.getenv();
//        myMap.forEach((k, v) -> System.out.println("Chave: " + k + ":" + " valor: " + v));
//

    }
}
